<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bms.roleodado">

    <application
        android:allowBackup="true"
        android:icon="@drawable/dice_1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dice_1"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoleODado">
        <activity android:name=".LemonActivity"  android:label="@string/game3"/>


        <activity android:name=".DiceActivity"   android:label="@string/game2" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"  android:label="@string/game1"/>
    </application>

</manifest>
